#include "main.h"
/**
 * _strlen - Write a function that returns the length of a string.
 * Return: Legth from the string
 * @s: String wich his length be computed
 */
int _strlen(char *s)
{
int a = 0;

for (; s[a] != '\0'; a++)
{
}
return (a);
}
