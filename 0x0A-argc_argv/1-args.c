#include "main.h"
/**
 * main - prints the number of arguments passed into it.
 * Return: zero
 * @argc: count
 * @argv: unused vector
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
printf("%d \n", argc - 1);
return (0);
}
